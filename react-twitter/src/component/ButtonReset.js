const ButtonReset = (props) => {
    return (
        <button 
            type="button"
            onClick={props.onClick}>
            全てをリセット
        </button>
    )
}

export default ButtonReset;